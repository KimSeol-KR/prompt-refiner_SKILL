# 변경 이력

되돌린 수정과 효과가 없던 수정도 함께 적습니다. 무엇이 통했는지보다 **무엇이 안 통했는지**가 다음 사람에게 더 쓸모 있기 때문입니다.

## 0.6.0 — 범위 재정렬

목적을 벗어난 규칙을 걷어냈습니다. 이 스킬의 대상은 **사용자가 쓴 요청문 하나**이고, 하는 일은 **거기 안 적힌 의도를 복원하는 것**입니다.

- **안전장치 성격 제거.** 전에는 "되돌릴 수 없으면 무조건 확인"이었습니다. 그런데 `"temp.log 지워줘"`는 하나도 모호하지 않은데 카드가 떴습니다. 판단 순서를 뒤집어, **먼저 결손이 있는지 보고 그 무게를 되돌리기 비용으로 재도록** 바꿨습니다. 되돌리기 비용은 저울이지 확인 사유가 아닙니다.
- **기본값 사전의 근거 재서술.** "결론 먼저", "의견 갈리면 양쪽 다", "새 의존성은 부채" 같은 값들이 *의도 추정*이 아니라 *품질 규범*으로 정당화돼 있었습니다. 근거를 둘로 한정했습니다 — ① 사용자가 그 칸을 채웠다면 이렇게 말했을 가능성 ② 틀렸을 때 되돌리기 쉬운가. **둘 중 어느 쪽으로도 정당화되지 않는 값은 기본값이 아니라 훈수입니다.**
- **경계 선언 추가.** `## 이 스킬이 하지 않는 일` 절을 앞쪽에 넣었습니다. 새 규칙을 넣기 전에 *"이게 사용자가 쓴 문장에서 의도를 복원하는 일인가"*를 먼저 묻게 합니다.
- `description` 수정 — "프롬프트 다듬어줘"라고 명시적으로 요청받으면 실행하지 않고 다듬은 요청문만 보여줍니다.

**되돌린 것**: 직전 버전에서 "에이전트가 스스로 산출물을 만들기 시작할 때도 확인한다"는 규칙을 넣었다가 뺐습니다. 그 자리에는 **보정할 사용자 입력이 아예 없어서** 보정이 아니라 에이전트 자기 규율입니다. 만든 사람의 실패를 스킬의 결함으로 일반화한 경우였습니다.

## 0.5.0 — 커넥터 탐색

- 0단계(묻기 전에 먼저 찾기)에 **연결된 도구**를 추가 — Notion·Drive·Slack 같은 커넥터와 저장된 메모리. 로컬 파일만 훑고 "재료가 없다"고 판정하는 실패를 막기 위한 것입니다.

**효과 미입증**: 3회씩 재검증했더니 수정본은 3/3 커넥터에 도달했는데 **수정 전 버전도 2/3 도달**했습니다. 차이가 노이즈 수준이라 이 문장의 효과는 입증되지 않았습니다. 해롭지 않아 남겨두었습니다.

## 0.4.0 — 사후 카드의 경계

- 0.3.0에서 사후 카드를 도입한 뒤 규칙이 너무 넓게 번졌습니다. 가벼운 작업뿐 아니라 되돌릴 수 없는 작업까지 샐 수 있는 상태였습니다.
- 경계를 명시: **"틀렸을 때 버리는 비용"** 으로 가른다. 버려도 아깝지 않으면 만들어서 보여주고, 아까우면 먼저 묻는다.

## 0.3.0 — 순서와 사후 카드

- **0단계 신설.** "찾을 수 있으면 찾는다"는 규칙은 있었는데 **언제** 찾는지가 없었습니다. 물어보기로 정한 뒤에 훑으면 사용자에게는 "찾아보지도 않고 물었다"로 보입니다.
- **사후 카드.** 대상 파일이나 코드를 못 봤으면 "만들 것"과 "가정"을 쓸 수가 없습니다. 억지로 쓰면 지어낸 소리가 됩니다. 이때는 질문만 던지고 **결과물을 만든 뒤에** 가정을 붙입니다.

## 0.2.0 — 첫 검증

케이스 4건으로 스킬 유무를 비교. 스킬 있음 20/20, 없음 5/20.

스킬 없는 쪽의 실패가 두 갈래로 갈렸습니다 — **확인 없이 실행한 뒤 사후 질문 4개**, 그리고 **질문 5묶음을 한 번에**. 막으려던 두 실패가 그대로 재현됐습니다.

## 0.1.0 — 최초

결손 7유형, 최대 2개 질문, 선택지 형태, 기본값 사전, "그냥 진행" 탈출구, 확인 카드 6단계 절차.

---

## 아직 안 푼 것

- **실사용 데이터가 0일입니다.** 지금까지의 케이스는 만든 사람이 만들고 만든 사람이 채점했습니다.
- **케이스당 실행이 1회**라 분산을 모릅니다.
- 채점 기준을 결과를 본 뒤에 손댄 적이 있습니다(카드 시점을 사전/사후 중립으로 변경). 정당한 수정이지만 측정이 결과에 맞춰졌을 위험이 남습니다.
